import styled from "styled-components"

export const ContainerWrapper = styled.div`
  overflow: hidden;
  display: flex;
  flex-direction: column;
  gap: 0 2rem;
  height: 100rem;
  position: absolute;


`
